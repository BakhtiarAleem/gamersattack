const Separator = (props) => {
    return (
        <>
            <div
                className={`${props.modification} mb-6 h-[1px] w-full bg-interface-700`}
            ></div>
        </>
    )
}

export default Separator
